server:
  port: 8880
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MVijayalakshmi2001/cloud-config.git
          search-paths:
          - '{application}'

management:
  endpoints:
    web:
      exposure:
        include:
        - "*"          